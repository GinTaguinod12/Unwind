go to: Gradle Scripts > build.gradle.kts (Module:app)

add this sa dependencies:
implementation("it.xabaras.android:recyclerview-swipedecorator:1.4")

what you should see:

dependencies { 
    implementation(libs.appcompat)
    implementation(libs.material)
    implementation(libs.activity)
    implementation("it.xabaras.android:recyclerview-swipedecorator:1.4") <---- You added this one
    implementation(libs.constraintlayout)
    testImplementation(libs.junit)
    androidTestImplementation(libs.ext.junit)
    androidTestImplementation(libs.espresso.core)
}

*** NOTE: DONT COPY THE WHOLE DEPENDENCIES FUNCTION, ONLY PASTE THE IMPLEMENTATION (...) CODE ***
